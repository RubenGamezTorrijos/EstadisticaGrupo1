La rama main(local) actualmente está funcionando correctamente y es la de producción, ten siempre en cuenta que esta rama siempre será la referente y objetivo para que la rama dev (local) la alcance entre los compañeros. Cómo Rafael ya ha completado su parte y está correctamente, faltaría que Bryann y Leslie completen la suya, todo esto en sería sólo en la rama dev (local). Cómo ahora mismo ya se ha integrado la nueva mejora al proyecto siguiente "Mejora 2" de añadir una variable como el índice de coste de vida por país. Pues ahora lo que quiero que hagas sin romper loque existe ahora es revisar lo siguiente:

1. Analiza al detalle "enunciado_practica.md" y confirmame si cumple todo lo que se solicita en la práctica en la rama main(local). Si no es así, dime que falta y lo implementamos, pero manteniendo lo existente y sin romper lo que ya existe y el estilo.
2. Cuando hayas terminado con el punto 1, me lo comunicas y me dices si hay que hacer algo más, si no, me lo comunicas y pasamos a la siguiente fase que sería la copiar la rama main(local) en la rama dev(local), pero sin completar las partes de Bryann y Leslie, ya que eso lo harían ellos hasta alcanzar el objetivo de la rama main(local), la parte de Rafael ya está completada actualmente en la rama dev(local) y se supone que en la rama main(local) ya está aplicado el nuevo cambio de rafael o incluso mejorado, con lo cual está si quedará ya implementada en la rama dev(local). Manten siempre el estilo y la forma de trabajo que se ha estado llevando hasta ahora y con la explicación que hace cada uno cómo está actualmente en el proyecto para seguir el mismo estilo y forma de trabajo, en la rama main(local), debe estar todo correcto y sin errores para que terminen bryann y leslie su parte. 
3. Recuerda que la rama main siempre será la referente de producción y la rama dev(local) es la que se usa para desarrollar y alcanzar para que sea lo mismo que la rama main(local) para conseguir el trabajo grupal y comprobar que todos trabajamos de la misma manera y con el mismo estilo su parte.

4. Actualmente como el proyecto tiene dos monedas USD $ y EUR €, me gustaría que se pudiese elegir en el menú lateral la moneda que se quiere usar para el proyecto, de manera que se pueda cambiar entre USD $ y EUR € y que se muestre en todos los gráficos y tablas la moneda que se está usando, además de que se pueda guardar la preferencia en una variable para que se pueda usar en todas las funciones del proyecto actuales y no afecte a otras dependencias.

5. Aplica siempre el código de buenas prácticas de programación, como por ejemplo:
- Comenta el código y define los "TODO" en ambas ramas (main y dev) local, sabiendo que main es la que siempre debe estar correcta y sin errores y se tomará siempre como referencia, definidiendo lo que hace cada rol también y cada función y los cálculos estádisticos que solicitan en la práctica "enunciado_practica.md"

6. Define las tareas listadas por ejemplo en README.md para saber que han hecho cada rol de los cuatro.